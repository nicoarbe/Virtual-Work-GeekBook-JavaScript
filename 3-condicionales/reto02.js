let userName = 'Juan Peláez';
const myName = 'Nicolás Arbeláez';
const hisName = 'Juan Peláez';

if (myName === userName || hisName === userName) {
    console.log(`Bienvenido, ${userName}`)
} else {
    console.log(`Lo siento pero el usuario que has introducido no está registrado`)
}